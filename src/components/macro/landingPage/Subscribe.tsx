/*IMPORT COMPONENTS*/
import Particle from '../../../particles/Particle';
/*IMPORT CSS*/
import { ArrowBox, Arrow } from './styledComponents';
import { Box } from '@mui/material';

const Subscribe = () => {
   return (
      <ArrowBox>
         <Arrow />
      </ArrowBox>
   );
};

export default Subscribe;
