/*IMPORT UTILITIES*/
import { styled } from '@mui/system';
import { Box } from '@mui/material';

export const NoAuthenticatedView = styled(Box)(
   ({ theme }) => `
  width: 100vw;
  height: 88.5vh;
  background-color: black;
`
);
